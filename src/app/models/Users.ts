import { Title } from '@angular/platform-browser';

export interface Users { 
    id?: string,
    name?: string,
    lastname?: string,
    email?:string,
    password?:string,
    server?:string,
};
 